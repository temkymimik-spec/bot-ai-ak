services:
  bot:
    build: .
    container_name: tg-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      # сессии и БД сохраняются между перезапусками
      - ./sessions:/app/sessions
      - ./bot.db:/app/bot.db
    environment:
      - PYTHONUNBUFFERED=1
